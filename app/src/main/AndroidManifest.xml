<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.myresume">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Education"/>
        <activity android:name=".Skill"></activity>
        <activity android:name=".Award" />
        <activity android:name=".School" />
        <activity android:name=".WorkExp" />
        <activity android:name=".ApplyforJob" />
        <activity android:name=".Project" />
        <activity android:name=".Hobby" />
        <activity android:name=".SelfAssess" />
        <activity android:name=".BasicInfor" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>